<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tistory.jeongs0222.namdaein">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.main.MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.splash.SplashActivity" />
        <activity
            android:name=".ui.activity.login.LoginActivity"
            android:theme="@style/LoginTheme" />
        <activity android:name=".ui.fragment.main.MainHomeFragment" />
        <activity android:name=".ui.fragment.main.MainMarketFragment" />
        <activity android:name=".ui.fragment.main.board.MainBoardFragment" />
        <activity android:name=".ui.fragment.main.MainMoreFragment" />
        <activity android:name=".ui.fragment.board.free.BoardFreeFragment" />
        <activity android:name=".ui.fragment.board.lost.BoardLostFragment" />
        <activity android:name=".ui.fragment.board.promote.BoardPromoteFragment" />
        <activity android:name=".ui.fragment.board.club.BoardClubFragment"/>
    </application>

</manifest>